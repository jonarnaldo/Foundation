{
  "name": "feature-agent-runner",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Runs one Claude Agent SDK session against the project, governed by coding_prompt.md, picking up the next failing feature from feature_list.json.",
  "scripts": {
    "start": "tsx run_agent_session.ts"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "latest"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.5.0"
  }
}